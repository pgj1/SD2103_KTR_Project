@model IEnumerable<KTR.Models.Recipes>
@using Microsoft.AspNetCore.Identity;
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager


@{
    ViewData["Title"] = "Recipes";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


    <div class="flextop">
    <h2>Recipes</h2>
    </div>

<div class="col-lg-12">
    <h4>
        Only registered users can view recipe details. <br />
        Become a member today to upload and view your recipes.
    </h4>
    <h4>
        <a asp-controller="RegisterAccount" asp-action="CreateUser">Register Now</a>
    </h4>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @* @Html.DisplayNameFor(model => model.RecipeName)*@ Recipe Name
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Servings)
            </th>
            @*
                 <th>
                    @Html.DisplayNameFor(model => model.LastUpdated)
                </th>
            *@
            <th>
                @*@Html.DisplayNameFor(model => model.CategoryId)*@ Food Category
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.MainId)*@ Main Ingredient
            </th>
            <!--<th>-->
                @*@Html.DisplayNameFor(model => model.StatusId)*@ <!--Visibility
            </th>-->

            <!--<th>-->
                @*@Html.DisplayNameFor(model => model.UserId)*@ <!--UserId
            </th>
            <th>Actions</th>-->

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)

        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.RecipeName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Servings)
                </td>
                @*           <td>
                        @Html.DisplayFor(modelItem => item.LastUpdated)
                    </td>
                *@
            <td>
                @Html.DisplayFor(modelItem => item.CatName.CatName)
                @*@Html.DisplayFor(modelItem => item.RecipeCategory.CatName)*@
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.MainName.MainName)
                </td>
                <!--<td>-->
                    @*@Html.DisplayFor(modelItem => item.StatusId)*@
                    <!--@Html.DisplayFor(modelItem => item.StatusName.StatusName)
                </td>-->
               
                <!--<td>-->
                    @*@Html.DisplayFor(modelItem => item.UserId)*@
                    <!--@Html.DisplayFor(modelItem => item.UserId)
                </td>
                <td>
                    | <a asp-action="Details" asp-route-id="@item.RecipeId">Details</a> |
                    <a asp-action="Edit" asp-route-id="@item.RecipeId">Edit</a> |-->
                    @*<a asp-action="Delete" asp-route-id="@item.RecipeId">Delete</a>*@
                <!--</td>-->

            </tr>

        }
    </tbody>
</table>

    